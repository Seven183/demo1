<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecdata.ec803.dao.RecordInfoDao">

    <select id="queryRecordInfo" resultType="hashmap" parameterType="hashmap">
        select t1.id "id",t1.police_office "policeOffice",t1.software_version "softwareVersion"
        from feiq_record_info t1
        where 1 = 1
        <if test="id !=null and id != ''">
            and t1.id = #{id}
        </if>
        order by t1.id
    </select>

    <select id="queryRecordInfo2" resultType="hashmap" parameterType="hashmap">
        select t1.police_asker1_name "policeAsker1Name", t1.person_name "personName",t1.createTime "createTime"
        from feiq_record_info t1
        where 1 = 1
        <if test="inquestId !=null and inquestId != ''">
            and t1.id = #{inquestId}
        </if>
    </select>

</mapper>
