<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecdata.ec803.dao.InquestDao">


    <select id="queryInquestComment" resultType="hashmap" parameterType="hashmap">
        select t1.titleName "kind",t1.sid "sid",t1.id "id",t1.time "time",t1.onebest "text",t1.msgXi "msgXi",
               t1.reliability "reliability"
        from feq_lattice t1
        where 1 = 1
        <if test="sid !=null and sid != ''">
            and t1.sid = #{sid}
        </if>
        <if test="startTime != null">
            and #{startTime} &lt; t1.time
        </if>
        <if test="endTime != null">
            and #{endTime} &gt; t1.time
        </if>
        order by t1.id
    </select>

    <update id="updateInquestLattice" parameterType="hashmap">
        update feq_lattice
        <set>
            <if test="text != null  and text != ''">
                onebest = #{text},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{latticeId}
    </update>


</mapper>
